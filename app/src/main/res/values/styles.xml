<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/putih</item>
        <item name="colorPrimaryDark">@color/hitam</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="toolbar_style">@style/AppTheme.Toolbar</item>
        <item name="android:windowTranslucentStatus">true</item>
    </style>

    <style name="AppTheme.Toolbar" parent="Widget.AppCompat.Toolbar">
        <item name="android:theme">@style/AppTheme.ThemeToolbar</item>
        <item name="android:popupTheme">@style/ThemeOverlay.AppCompat.Light</item>
    </style>

    <style name="AppTheme.ThemeToolbar" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:textColorPrimary">@color/abu_putih</item>
        <item name="android:textColorPrimaryInverse">@color/abu_putih</item>
        <item name="android:actionMenuTextColor">@color/abu_putih</item>
    </style>

</resources>
